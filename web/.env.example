# Public values are compiled into the browser bundle during `npm run build`.
# RPC variables accept one or more comma-separated HTTP(S) endpoints; configure
# at least two providers per production chain for resilient reads.
NEXT_PUBLIC_SITE_URL=http://localhost:3002
NEXT_PUBLIC_BENDYSTRAW_URL=https://bendystraw.up.railway.app
NEXT_PUBLIC_TESTNET_BENDYSTRAW_URL=https://testnet.bendystraw.xyz
NEXT_PUBLIC_PARA_API_KEY=beta_bfd2fb77a3fc475f00918565f2f863e3
NEXT_PUBLIC_PARA_ENV=BETA
# Which Para on-ramp provider the headless "Get ETH" call bills through.
# Must be one live in the Para dashboard: STRIPE | MOONPAY | RAMP | CDP | MERCURYO.
NEXT_PUBLIC_PARA_ONRAMP_PROVIDER=CDP
# WalletConnect Cloud project id. Leave empty to hide the WalletConnect
# option entirely — the relay rejects unregistered ids, so a connector
# without one is a button that always fails.
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=
# Immutable release identifier, normally the Railway/Git commit SHA.
NEXT_PUBLIC_VERSION=development
# Optional verified Base factory address for direct recovery during gateway outages.
NEXT_PUBLIC_TELLIGENCE_FACTORY_ADDRESS=
# Server-only gateway origin. Browsers use the bounded same-origin proxy.
TELLIGENCE_GATEWAY_URL=http://localhost:8080

# IPFS writes and RPC reads go directly from this trusted origin to Juicebox Center.
# Do not put a Juicebox Center API key in this client-side environment.
