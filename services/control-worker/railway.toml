[build]
builder = "DOCKERFILE"
dockerfilePath = "services/Dockerfile"

[deploy]
startCommand = "node control-worker/server.mjs"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
