# Telligence browser screenshots

Captured from the production standalone browser verification build at 1280, 1100, 768, 390, and 320 pixel widths. The filename identifies the route family and viewport.

- `home-*`: a deterministic directory containing “The open archive” with observed fixture credit and “A patient garden” awaiting verification.
- `project-*`: the same archive fixture, showing daily credit, remaining credit, the separate funding target, supporter rights, and inspection links.
- `create-*`: a draft at the immutable policy review, with an unconnected creator wallet. Policy values are test fixtures.
- `keys-*`: the disconnected key console. No real API key or secret is present.

These images verify responsive presentation and readable states. The named projects, addresses, credit amounts, and policy values are fixtures defined in `web/test/e2e/`; they are not seeded into the application, presented as a deployment, or used as evidence of live provider capacity.
